import {normalizeCardNumber} from "./normalizeCardNumber";

export const isCardValidByLuhnsAlgorithm = (cardNumber) => {
    const normalized = normalizeCardNumber(cardNumber);
    // ... @TODO dopisać logikę algorytmu
}